{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "build:docker-image",
			"command": "python",
			"args": ["docker-build.py"],
			"detail": "Builds the Docker image, with pre-set project name, and current time as the tag"
		}
	]
}